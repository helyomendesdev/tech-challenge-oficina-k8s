apiVersion: v1
kind: Service
metadata:
  name: oficina-app
  namespace: oficina

spec:
  selector:
    app: oficina-app

  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
      protocol: TCP

  type: NodePort
